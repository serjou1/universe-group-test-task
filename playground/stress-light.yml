config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - flow:
      - post:
          url: "/topics/test"
          json: 
            message: "Hello, World!"